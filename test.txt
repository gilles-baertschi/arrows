type Id a b = undefined

type List a = (() | (a, List a))
type String = List Char

instance Show Int where
    show = undefined

class Show a where
    show :: a -> String

toUpper :: String -> String
toUpper = undefined

myDefinition :: (Show a) => Int -> a
myDefinition = show

